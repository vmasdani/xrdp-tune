# editor and backup files
*.swp
*.swo
*~
# ovpn profiles and the installer's backups: never commit real credentials/IPs
*.ovpn
*.ovpn.orig
*.ovpn.bak
# WireGuard profiles carry private keys; vpn-split.conf is the only .conf tracked
*.conf
!vpn-split.conf
